server:
  port: 8083

spring:
  application:
    name: ai-concierge
  ai:
    vertexai:
      gemini:
        project-id: ${GOOGLE_CLOUD_PROJECT_ID:nexusops-demo}
        location: ${GOOGLE_CLOUD_LOCATION:us-central1}
        chat:
          options:
            model: gemini-pro
